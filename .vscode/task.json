{
    "tasks": [
      {
        "label": "JupyText Sync",
        "type": "shell",
        "command": "jupytext --sync ${file} notebooks/*",
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "presentation": {
          "reveal": "never",
        }
      }
    ]
  }